Run the canonical PrisM PR review workflow on the given PR number, branch, or commit.

First, read and internalize the skill definition and repo rules:

```
Read: .prism/skills/pr-review/SKILL.md
Read: .prism/review-rules.toml
```

Then follow the 8-step workflow in SKILL.md exactly, treating the loaded `review-rules.toml` as the repo-specific rules for Step 0.

Arguments: $ARGUMENTS
