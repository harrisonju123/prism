diff --git a/crates/uglyhat-panel/src/agent_roster_panel.rs b/crates/uglyhat-panel/src/agent_roster_panel.rs
index da89b17..825712a 100644
--- a/crates/uglyhat-panel/src/agent_roster_panel.rs
+++ b/crates/uglyhat-panel/src/agent_roster_panel.rs
@@ -7,6 +7,8 @@ use gpui::{
     Window,
 };
 use serde::{Deserialize, Serialize};
+use std::path::PathBuf;
+use std::sync::Arc;
 use std::time::Duration;
 use ui::{
     h_flex, prelude::*, v_flex, Button, ButtonStyle, Color, IconButton, IconName, Label, LabelSize,
@@ -15,7 +17,7 @@ use ui::{
 use util::{ResultExt, TryFutureExt};
 use workspace::{
     dock::{DockPosition, Panel, PanelEvent},
-    Workspace,
+    open_paths, AppState, OpenOptions, Workspace,
 };
 
 const PANEL_KEY: &str = "AgentRosterPanel";
@@ -27,7 +29,9 @@ actions!(
         /// Toggles the agent roster panel.
         Toggle,
         /// Toggles focus on the agent roster panel.
-        ToggleFocus
+        ToggleFocus,
+        /// Spawns a new Claude Code agent in a new git worktree and opens a Zed window there.
+        SpawnAgentInWorktree,
     ]
 );
 
@@ -56,6 +60,8 @@ pub struct AgentRosterPanel {
     _auto_refresh: Task<()>,
     pending_serialization: Task<Option<()>>,
     send_task: Option<Task<()>>,
+    spawn_error: Option<String>,
+    spawn_task: Option<Task<()>>,
 }
 
 #[derive(Serialize, Deserialize)]
@@ -89,6 +95,8 @@ impl AgentRosterPanel {
                 _auto_refresh: Task::ready(()),
                 pending_serialization: Task::ready(None),
                 send_task: None,
+                spawn_error: None,
+                spawn_task: None,
             };
 
             let auto_refresh = cx.spawn(async move |this, cx| loop {
@@ -271,6 +279,83 @@ impl AgentRosterPanel {
         }));
     }
 
+    pub fn spawn_worktree_agent(
+        &mut self,
+        app_state: Arc<AppState>,
+        repo_root: PathBuf,
+        cx: &mut Context<Self>,
+    ) {
+        self.spawn_error = None;
+        cx.notify();
+
+        self.spawn_task = Some(cx.spawn(async move |this, cx| {
+            let now = std::time::SystemTime::now()
+                .duration_since(std::time::UNIX_EPOCH)
+                .unwrap_or_default();
+            let secs = now.as_secs();
+            let name = format!("agent-{secs}");
+            let wt_path = repo_root.join(".worktrees").join(&name);
+
+            let result: anyhow::Result<()> = async {
+                let git_status = cx
+                    .background_spawn({
+                        let repo_root = repo_root.clone();
+                        let name = name.clone();
+                        let wt_path = wt_path.clone();
+                        async move {
+                            std::process::Command::new("git")
+                                .arg("worktree")
+                                .arg("add")
+                                .arg(&wt_path)
+                                .arg("-b")
+                                .arg(&name)
+                                .current_dir(&repo_root)
+                                .status()
+                        }
+                    })
+                    .await?;
+                if !git_status.success() {
+                    anyhow::bail!("git worktree add failed");
+                }
+
+                cx.background_spawn({
+                    let repo_root = repo_root.clone();
+                    let name = name.clone();
+                    async move {
+                        std::process::Command::new(uh_binary())
+                            .args(["task", "create", &name, "--status", "in_progress"])
+                            .current_dir(&repo_root)
+                            .status()
+                            .ok();
+                    }
+                })
+                .await;
+
+                let open_task = cx.update(|app_cx| {
+                    open_paths(&[wt_path], app_state, OpenOptions::default(), app_cx)
+                });
+                open_task.await?;
+
+                anyhow::Ok(())
+            }
+            .await;
+
+            this.update(cx, |this, cx| {
+                this.spawn_task = None;
+                match result {
+                    Ok(()) => {
+                        this.refresh(cx);
+                    }
+                    Err(e) => {
+                        this.spawn_error = Some(e.to_string());
+                    }
+                }
+                cx.notify();
+            })
+            .ok();
+        }));
+    }
+
     fn render_agent_row(&self, agent: &AgentStatus, cx: &mut Context<Self>) -> impl IntoElement {
         let dot_color = if agent.session_open {
             Color::Success
@@ -504,11 +589,28 @@ impl Render for AgentRosterPanel {
                             .size(LabelSize::Small),
                     )
                     .child(
-                        IconButton::new("refresh", IconName::ArrowCircle)
-                            .icon_size(ui::IconSize::Small)
-                            .icon_color(Color::Muted)
-                            .tooltip(Tooltip::text("Refresh"))
-                            .on_click(cx.listener(|this, _, _, cx| this.refresh(cx))),
+                        h_flex()
+                            .gap_1()
+                            .child(
+                                IconButton::new("spawn-agent", IconName::Plus)
+                                    .icon_size(ui::IconSize::Small)
+                                    .icon_color(Color::Muted)
+                                    .tooltip(Tooltip::text("Spawn agent in new worktree"))
+                                    .disabled(self.spawn_task.is_some())
+                                    .on_click(cx.listener(|_, _, window, cx| {
+                                        window.dispatch_action(
+                                            SpawnAgentInWorktree.boxed_clone(),
+                                            cx,
+                                        );
+                                    })),
+                            )
+                            .child(
+                                IconButton::new("refresh", IconName::ArrowCircle)
+                                    .icon_size(ui::IconSize::Small)
+                                    .icon_color(Color::Muted)
+                                    .tooltip(Tooltip::text("Refresh"))
+                                    .on_click(cx.listener(|this, _, _, cx| this.refresh(cx))),
+                            ),
                     ),
             )
             .child(
@@ -524,6 +626,27 @@ impl Render for AgentRosterPanel {
                                 .child(Label::new(err).size(LabelSize::Small).color(Color::Error)),
                         )
                     })
+                    .when_some(self.spawn_error.clone(), |this, err| {
+                        this.child(
+                            div()
+                                .px_2()
+                                .py_1()
+                                .child(
+                                    Label::new(format!("Spawn failed: {err}"))
+                                        .size(LabelSize::Small)
+                                        .color(Color::Error),
+                                ),
+                        )
+                    })
+                    .when(self.spawn_task.is_some(), |this| {
+                        this.child(
+                            div().px_2().py_1().child(
+                                Label::new("Spawning agent…")
+                                    .size(LabelSize::Small)
+                                    .color(Color::Muted),
+                            ),
+                        )
+                    })
                     .when(self.is_loading && agents.is_empty(), |this| {
                         this.child(
                             div().px_2().py_1().child(
diff --git a/crates/uglyhat-panel/src/uglyhat_panel.rs b/crates/uglyhat-panel/src/uglyhat_panel.rs
index ea32ef9..48859d9 100644
--- a/crates/uglyhat-panel/src/uglyhat_panel.rs
+++ b/crates/uglyhat-panel/src/uglyhat_panel.rs
@@ -9,7 +9,7 @@ pub use approval_gate::{ApprovalDecision, ApprovalGate};
 pub use session_history_panel::SessionHistoryPanel;
 pub use task_board_panel::TaskBoardPanel;
 
-use agent_roster_panel::{Toggle as RosterToggle, ToggleFocus as RosterToggleFocus};
+use agent_roster_panel::{SpawnAgentInWorktree, Toggle as RosterToggle, ToggleFocus as RosterToggleFocus};
 use gpui::App;
 use session_history_panel::{Toggle as HistoryToggle, ToggleFocus as HistoryToggleFocus};
 use task_board_panel::{Toggle, ToggleFocus};
@@ -43,6 +43,24 @@ pub fn init(cx: &mut App) {
                 workspace.close_panel::<SessionHistoryPanel>(window, cx);
             }
         });
+
+        workspace.register_action(|workspace, _: &SpawnAgentInWorktree, _window, cx| {
+            let app_state = workspace.app_state().clone();
+            let Some(repo_root) = workspace
+                .project()
+                .read(cx)
+                .visible_worktrees(cx)
+                .next()
+                .map(|wt| wt.read(cx).abs_path().to_path_buf())
+            else {
+                return;
+            };
+            if let Some(panel) = workspace.panel::<AgentRosterPanel>(cx) {
+                panel.update(cx, |panel, cx| {
+                    panel.spawn_worktree_agent(app_state, repo_root, cx);
+                });
+            }
+        });
     })
     .detach();
 }
